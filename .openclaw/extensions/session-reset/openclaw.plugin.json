{
  "id": "session-reset",
  "name": "Session Reset API",
  "description": "Provides a universal HTTP endpoint for resetting OpenClaw sessions across all messengers",
  "version": "1.0.0",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "authToken": {
        "type": "string",
        "description": "Bearer token for API authentication (defaults to GATEWAY_TOKEN env var)"
      },
      "requireAuth": {
        "type": "boolean",
        "default": true,
        "description": "Whether to require authentication for reset endpoint"
      }
    }
  },
  "uiHints": {
    "authToken": {
      "label": "Auth Token",
      "help": "Bearer token for API authentication. If not set, uses GATEWAY_TOKEN environment variable.",
      "sensitive": true
    },
    "requireAuth": {
      "label": "Require Authentication",
      "help": "When enabled, the endpoint requires a valid Bearer token in the Authorization header"
    }
  }
}